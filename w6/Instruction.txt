# Notes
# * all tests require Python >= 3.4
# * in order to test code,
#   you should copy it in directory with tests

# 1. Run presubmit tests (without bonus)
nosetests task3_presubmit_tests.py